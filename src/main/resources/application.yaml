spring:
  application:
    name: clash-bot-service
  jpa:
    hibernate:
      ddl-auto: update
  devtools:
    restart:
      enabled: false
logging:
  level:
    root: INFO
    com.poss: INFO
    org.springframework.data: DEBUG
