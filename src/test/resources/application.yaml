spring:
  application:
    name: clash-bot-service
  jpa:
    hibernate:
      ddl-auto: update
logging:
  level:
    root: DEBUG
    com.poss: DEBUG
    org.springframework: DEBUG
    reactor.core.publisher: TRACE
